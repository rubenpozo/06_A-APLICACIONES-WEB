@model IEnumerable<Evaluacion_Parcial_1.Models.Libro>

@{
    ViewData["Title"] = "Index";
}

<h1>Libros</h1>

<p>
    <a asp-action="Create">Nuevo Libro</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Titulo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Genero)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaPublicacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ISBN)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Autor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(model => item.Titulo)
            </td>
            <td>
                @Html.DisplayFor(model => item.Genero)
            </td>
            <td>
                @Html.DisplayFor(model => item.FechaPublicacion)
            </td>
            <td>
                @Html.DisplayFor(model => item.ISBN)
            </td>
            <td>
                @item.Autor.Nombre @item.Autor.Apellido
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LibroId">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.LibroId">Detalles</a> |
                <a asp-action="Delete" asp-route-id="@item.LibroId">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
