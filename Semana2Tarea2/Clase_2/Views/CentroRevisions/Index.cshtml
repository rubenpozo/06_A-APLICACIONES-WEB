@model IEnumerable<Clase_2.Models.CentroRevision>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProvinciaId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Provincia)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(model => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(model => item.ProvinciaId)
            </td>
            <td>
                @Html.DisplayFor(model => item.Provincia)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CentroRevisionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CentroRevisionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CentroRevisionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
